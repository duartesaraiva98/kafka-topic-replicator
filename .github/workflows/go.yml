# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on: 
  push:
    branches:
    - 'main'
    tags:
    - '*'

jobs:

  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Set up dependencies
      run: cd src && go get github.com/confluentinc/confluent-kafka-go/v2 && go get github.com/spf13/viper

    - name: Build linux-amd64
      env:
        GOOS: linux
        GOARCH: amd64
      run: go build -v -o target/kafka-topic-replicator-$GOOS-$GOARCH

    - name: Build linux-arm64
      env:
        GOOS: linux
        GOARCH: arm64
      run: go build -v -o target/kafka-topic-replicator-$GOOS-$GOARCH

    - name: Print all that will be released
      run: ls target

    # - name: Creating a release
    #   uses: ncipollo/release-action@v1
    #   with:
    #     artifacts: "src/target/*"
